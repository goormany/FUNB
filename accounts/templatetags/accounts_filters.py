# from django import template
# from accounts.models import Profile
# register = template.Library()
#
#
# # Через шаблон можно получать данные профайла, который передается через request.
# # value нужно передавать в ковычках
# @register.filter
# def get_profile_fields(request, value):
#     field = Profile.objects.get(user=request.user)
#     return getattr(field, value)









